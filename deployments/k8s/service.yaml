apiVersion: v1
kind: Service
metadata:
  name: nkorebank
  namespace: nkore-bank
  labels:
    app: nkorebank
spec:
  type: ClusterIP
  selector:
    app: nkorebank
  ports:
    - name: http
      port: 80
      targetPort: 8080
      protocol: TCP
